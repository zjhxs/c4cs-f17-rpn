language: python
python:
  - "3.5"
# command to install dependencies
# install:
#   - pip install -r requirements.txt
# command to run tests
script:
  - python3 test_rpn.py # or py.test for Python versions 3.5 and below
notifications:
  email: false
